name: UnbreakableHouse # The name of your plugin
author: "tinkeringidiot" # Your username.
main: com.tinkeringidiot.unbreakablehouse.UnbreakableHouse # Replace "me.mfnalex" with the Group ID you chose, and adjust "helloworldplugin.HelloWorldPlugin" if you chose another plugin name.
version: ${project.version} # Maven will automatically replace this with the version number defined in your pom.xml
api-version: "1.13" # Optional, but you should add this when your plugin should run on ANY versions above 1.13

commands:
  protect:
    description: Protect a named region
    usage: /<command> <region name>